# Copie este arquivo para ".env" e ajuste os valores.

# Porta em que o servidor WebSocket vai escutar
WS_PORT=8080

# DEVE ser IDÊNTICO ao valor de WS_SHARED_SECRET em backend/config.php
WS_SHARED_SECRET=troque-este-segredo-em-producao-32chars
